<!DOCTYPE html>
<html lang="en">

<head>
  <title>Admin Check Order Page</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" type="text/css" />
  <link rel="stylesheet" href="/vendor/border-box.css" type="text/css" />
  <!-- <link rel="stylesheet" href="/styles/main.css" type="text/css" /> -->
  <link rel="stylesheet" href="/styles/layout.css" type="text/css" />

  <script type="text/javascript" src="/vendor/jquery-3.0.0.js"></script>
  <script type="text/javascript" src="/scripts/app.js"></script>
</head>

<body>
  <div class="header">
    <a class="link" href="/newitem">Create New Item</a>
  </div>
  <div class="orders-wrapper">
    <!-- 完成イメージ -->
    <!-- <div class="order-information">
      <h4 class="order-header">
        <span class="order-id">1</span>
        <span class="order-name">Steve</span>
        <span class="order-datetime">12:15</span>
        <span class="order-totalprice">$5</span>
      </h4>
      <div class="food-detail">
        <span class="food-item-name">latte</span>
        <span class="quantity">1</span>
      </div>
      <div class="food-detail">
        <span class="food-item-name">cappuccino</span>
        <span class="quantity">2</span>
      </div>
      <div class="food-detail">
        <span class="food-item-name">green tea flapetino</span>
        <span class="quantity">2</span>
      </div>
    </div> -->

    <%#= error %>
    <!-- 実際の表示コード -->
    <% for(let order of adminOrders) { %>
    <div class="order-information">
      <h4 class="order-header">
        <span class="order-id"><%= order.id %></span>
        <span class="order-name"><%= order.name %></span>
        <span class="order-time"><%= order.time %></span>
        <span class="order-totalprice">$<%= order.totalprice %></span>
      </h4>
      <% for(let item of order.items) { %>
      <div class="food-detail">
        <span class="food-item-name"><%= item.itemname %></span>
        <span class="quantity"><%= item.quantity%></span>
      </div>
      <% } %>
    </div>
    <% } %>
  </div>
  <style>
    .order-information {
      border: solid 1px #ccc;
      padding: 10px;
    }

    .order-information .order-header {
      margin: 0;
      border-bottom: solid 1px;
      width: 15em;
    }

    .food-detail {
      margin-left: 1em;
    }

    .food-detail .food-item-name {
      display: inline-block;
      width: 12em;
    }
  </style>
</body>

</html>